
export default function LoginLayout({ children }) {
  return (
    <div className="flex flex-col items-center justify-center 
        bg-gradient-to-r from-[#ff9a56] to-[#ffb366]
      mx-auto  shadow-lg h-screen">
      {children}
    </div>
  );
}
